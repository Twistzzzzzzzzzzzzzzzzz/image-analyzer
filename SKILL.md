---
name: image-analyzer
description: |
  Analyze and describe image content with structured output templates. MUST use this skill whenever the user:
  - Asks about any image, screenshot, photo, picture, chart, diagram, or UI mockup
  - Wants to know "what's in this image" or "what does this picture show"
  - Shares a file path ending in .png .jpg .jpeg .gif .webp .bmp .svg .avif .tiff
  - Says anything like "看看这个图", "分析这张截图", "这张照片里有什么", "帮我识别图片", "提取图片里的文字", "这个图表什么意思", "看看这个界面", "这个设计怎么样", "图片上写了什么", "这张图说明了什么", "review this UI", "what's in this screenshot", "analyze this chart", "extract text from this image", "describe this photo", "tell me about this picture", "before and after", "对比这两张", "这图有什么问题"
  - Shares a screenshot for debugging or code review purposes
  - Wants OCR / text extraction from any image
  - Reviews UI designs, evaluates layout, or checks visual design quality
  - Interprets data visualizations, flowcharts, architecture diagrams, or mind maps
  - Compares multiple images (before/after, version A/B, different states)
  
  Do NOT skip this skill when the user mentions any image file or asks to "look at" something visual — even if the request seems simple. Always use the structured templates for consistent, professional output.
---

# Image Analyzer

分析图片内容，支持截图分析、UI 审查、图表解读、OCR 文字提取、照片描述、对比分析六大场景。使用 Read 工具读取图片文件（Claude 原生多模态能力），并按场景输出结构化分析结果。

## 核心原则

- **先识别场景，再套用模板**：读图后按"场景识别决策树"判断类型，然后使用对应的输出模板
- **所有输出使用中文**，原文引用除外
- **先描述再分析**：先客观描述画面内容，再做主观解读
- **不确定时标注**：无法确认的内容加 `[不确定]` 标记
- **输出后自检**：完成后对照"自我检查清单"确认质量

## 图片读取

使用 Read 工具直接读取图片文件路径，支持 PNG、JPG、JPEG、GIF、WebP、BMP、SVG、AVIF、TIFF。

```
Read(file_path="用户提供的图片路径")
```

对于 URL 图片，先用 curl/wget 下载到临时目录，再 Read 读取。

**读取失败时**：
- 检查文件是否存在、扩展名是否正确
- 尝试用 `file` 命令确认文件类型（Linux/Mac）
- 如果确实不可读，告知用户并提供可能原因（文件损坏、格式不支持、路径错误）
- 不要编造内容，不要猜测

### 批量处理

1. 一次性并行读取所有图片（Read 工具支持并行调用）
2. 每张图片独立分析后，做整体对比总结
3. 图片超过 10 张时，先快速预览列出缩略图概要，询问用户优先分析哪些
4. 同一批图片来自同一场景时（如同一页面的不同状态），自动启用"对比分析"模式

## 场景识别决策树

读图后按以下顺序判断场景类型：

```
图片里主要是文字？
  ├─ 是 → 场景 4：OCR 文字提取
  └─ 否 → 是软件/网页/App 界面？
            ├─ 是 → 是设计稿/原型图？
            │         ├─ 是 → 场景 2：UI 审查
            │         └─ 否 → 场景 1：截图分析
            └─ 否 → 是数据图表/流程图/架构图？
                      ├─ 是 → 场景 3：图表解读
                      └─ 否 → 用户要求对比两张以上图片？
                                ├─ 是 → 场景 6：对比分析
                                └─ 否 → 场景 5：照片描述
```

**混合场景**：一张图片命中多个场景时（如截图内嵌图表），按以下策略：
1. 使用占比最大的场景作为主模板
2. 在报告中添加副场景小节（如截图分析报告中加入"### 内嵌图表"）
3. 用户明确关注某一方面时，以用户意图为准

## 六种场景与分析模板

### 1. 截图分析（Screenshot）

适用于：软件界面截图、网页截图、终端截图、App 截图、错误弹窗

**输出结构：**

```
## 截图分析

### 基本信息
- 平台/系统：[Windows/macOS/Linux/iOS/Android/Web]
- 应用/网站：[应用名称，可识别时填写]
- 截图区域：[全屏/窗口/局部]

### 界面布局
描述各区域位置和内容，从上到下、从左到右

### 关键元素
| 元素类型 | 内容 | 位置 |
|----------|------|------|
| 按钮     | xxx  | 顶部 |
| 输入框   | xxx  | 中部 |
| ...      | ...  | ...  |

### 文字内容
逐条列出所有可见文字，保留原文。代码和终端输出用代码块展示。

### 状态与交互
- 当前状态：[正常/错误/加载中/空状态/警告/成功/...]
- 可能操作：[描述用户在当前界面可以做什么]

### 问题诊断（错误截图专属）
- 错误类型：[编译错误/运行时异常/网络错误/权限问题/...]
- 错误位置：[文件名:行号，如可识别]
- 可能原因：[分析最可能的原因]
- 建议操作：[具体的修复步骤]
```

### 2. UI 审查（UI Review）

适用于：设计稿截图、Figma 导出图、UI 稿、落地页截图

**输出结构：**

```
## UI 审查

### 设计概览
- 页面类型：[首页/详情页/表单/仪表盘/设置页/登录页/...]
- 设计风格：[扁平/拟物/新拟态/极简/玻璃态/...]
- 主色调：[列出具体颜色值或描述]
- 是否响应式：[桌面端/平板/移动端视图]

### 可用性评估
| 维度 | 评分(1-5) | 说明 |
|------|-----------|------|
| 视觉层次 | x | 具体观察 |
| 导航清晰度 | x | 具体观察 |
| 操作可达性 | x | 具体观察 |
| 信息密度 | x | 具体观察 |
| 色彩对比度 | x | 文字/背景对比是否足够 |
| 一致性 | x | 同类元素风格是否统一 |

### 优点
- [具体优点，说出为什么好]

### 改进建议
1. 🔴 高优先：[具体问题和可操作的建议]
2. 🟡 中优先：[具体问题和可操作的建议]
3. 🟢 低优先：[具体问题和可操作的建议]

### 排版细节
- 字体：[可识别的字体系列和风格]
- 字号层级：[标题/正文/辅助文本的字号感知]
- 间距：[感受到的内边距、外边距、行距特点]
- 圆角/阴影：[若有]
```

### 3. 图表解读（Chart & Diagram）

适用于：数据图表（柱状图/折线图/饼图/散点图/热力图）、流程图、架构图、思维导图、UML 图、时序图、ER 图

**输出结构：**

```
## 图表解读

### 图表类型
[具体类型，尽量精确]

### 数据解读（数据图表适用）
- 标题：[图表标题]
- 横轴：[含义，含单位]
- 纵轴：[含义，含单位]
- 图例：[每个系列的含义]
- 数据趋势：[整体趋势描述，用具体数值]
- 关键数据点：[极值、拐点、交叉点及其含义]
- 异常点：[异常数据及可能的业务含义]

### 结构与关系（流程图/架构图适用）
- 主要节点：[列出并说明每个节点的角色]
- 层级关系：[父子、调用、依赖关系]
- 数据流向：[数据/请求/信息如何流动，方向用 → 标注]
- 关键路径：[最重要的路径，如核心业务流程]
- 瓶颈/单点：[架构图中看起来脆弱的地方]

### 核心信息
用一句话概括这张图想传达什么

### 数据可信度
- 数据来源：[图中是否标注了来源]
- 时间范围：[数据覆盖的时间]
- 是否完整：[有没有看起来被截断或省略的部分]
```

### 4. OCR 文字提取（OCR）

适用于：扫描件、文档照片、PPT 截图、菜单、招牌、书籍页面等

**输出结构：**

```
## 文字提取结果

### 提取文本
[逐段输出所有文字，保留原文语言。代码/表格/列表尽量保留格式]

### 文字统计
- 总字数：约 xx 字
- 语言：[中文/英文/中英混合/日文/韩文/...]

### 结构调整
[对提取出的文字做合理分段和标题识别，注明推断的标题层级]

### 识别置信度
- 清晰区域：[100% 可辨的部分]
- 模糊/遮挡区域：[用小字号或 [...] 标注不确定的部分]
- 手写文字：[如存在手写字，单独标注并说明识别难度]
```

### 5. 照片描述（Photo）

适用于：生活照片、风景照、产品照片、人物照片、动物照片等

**输出结构：**

```
## 照片描述

### 场景概述
用 2-3 句话概括画面内容

### 主体识别
- 主要物体：[列出并简要描述]
- 人物：[数量、大致年龄范围、外貌特征、动作姿态。不猜测具体身份]
- 动物/植物：[种类（如可辨识）、状态]

### 环境与背景
- 地点类型：[室内/室外/无法判断，更具体更好（餐厅/办公室/街道/海滩...）]
- 时间感知：[白天/夜晚/黄昏/黎明，从光线判断]
- 光线条件：[自然光/人造光/混合/逆光/侧光/...]
- 背景细节：[描述背景中值得注意的内容]

### 技术细节
- 色彩：[主色调、饱和度、冷暖倾向]
- 构图：[中心/三分法/对称/引导线/框架/...]
- 景深：[浅景深/深景深/主体突出程度]
- 值得注意的细节：[有趣或异常的细节]

### 情绪与氛围
描述画面传达的情绪感受，用客观词汇（宁静、紧张、欢快、压抑...），不做文学化过度解读
```

### 6. 对比分析（Comparison）*新增*

适用于：before/after 截图、A/B 设计稿、版本迭代对比、不同状态截图

触发词："对比"、"比较"、"before and after"、"改前改后"、"两个版本"、"有什么区别"

**输出结构：**

```
## 对比分析

### 对比概览
| 维度 | 图片 A | 图片 B |
|------|--------|--------|
| 简要描述 | xxx | xxx |
| 场景类型 | xxx | xxx |

### 相同点
- [列出两张图的共同之处]

### 差异点
| 变化项 | 图片 A | 图片 B | 变化说明 |
|--------|--------|--------|----------|
| xxx    | xxx    | xxx    | xxx      |

### 变化评估
- 改进项：[哪些变化是正向的]
- 退步项：[哪些变化可能有问题]
- 未解决问题：[两张图共存的缺陷]

### 推荐判断
如果这是 before/after 对比：A → B 是改善还是恶化？给出明确判断和理由。
```

## 输出格式选择

根据用户指令选择输出详细程度：

- **详细报告（默认）**：使用上述完整模板。用户说"分析/详细/审查/看看"时使用
- **简短描述**：只输出场景概述 + 核心要点（3-5 条）。用户说"大概看看/简述/快速看下/quick"时使用
- **纯文字提取**：OCR 场景下只输出提取的文本。用户说"提取文字/只要文字/OCR"时使用
- **纯诊断**：错误截图场景下只输出问题诊断部分。用户说"这个报错怎么回事/帮我看看这个bug"时使用

## 自我检查清单

每次输出前，快速确认：

- [ ] 场景识别是否正确？（用决策树复核）
- [ ] 模板是否完整？（没漏掉必要的字段）
- [ ] 混合场景是否处理？（截图里有图表？加了副场景小节？）
- [ ] 不确定的地方标注了 `[不确定]` 了吗？
- [ ] 有隐私信息需要提醒用户吗？（人脸、手机号、证件号、地址）
- [ ] 输出用中文了吗？（原文引用除外）
- [ ] 图片超过 2 张时，开启对比模式了吗？

## 注意事项

- 一张图命中多个场景时，主模板 + 副场景小节，而不是两张皮
- 遇到文字始终提取原文，分析用中文
- `[不确定]` 远比瞎猜强——用户宁愿看到一个诚实的标记，也不愿意被误导
- 涉及人脸、证件号、手机号、地址等隐私信息时，在报告末尾加一条"隐私提醒"
- 代码截图中的代码片段，优先判断语言并给出语法高亮的代码块
- 如果图片明显是 AI 生成（Midjourney/Stable Diffusion/DALL·E 风格特征），在报告中注明
